<div class="container mt-4">
    <div class="row">

        <!-- Colonne gauche : menu -->
        <div class="col-3">
            <div class="bg-light border rounded-3 p-3 shadow-sm">
                <h6 class="text-secondary text-uppercase">Menu</h6>
                <div class="list-group list-group-flush">
                    <?php foreach ($this->menuItems as $item): ?>
                    <a href="<?= $item['url'] ?>" class="list-group-item list-group-item-action menu-link">
                        <?= $this->escape($item['label']) ?>
                    </a>
                    <?php endforeach; ?>
                </div>
            </div>
        </div>

        <!-- Colonne droite : contenu principal -->
        <div class="col-9">
            <h2>Module</h2>
            <?php echo $this->message; ?>
            <!-- Ici tu peux mettre tout le contenu spécifique à l'action -->
        </div>

    </div>
</div>